# cc-supervisor (`ccs`)

Python half of CC Supervisor: the `ccs` CLI, launcher, daemon, and statusline.
See `../docs/manual/` for user docs and `../docs/decisions/` for design rules.

```sh
make venv        # python/.venv with dev tools
make test lint
make install-dev # pipx editable install → ~/.local/bin/ccs
```
